import { MaxBagagesDirective } from './max-bagages.directive';

describe('MaxBagagesDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxBagagesDirective();
    expect(directive).toBeTruthy();
  });
});
